## Packages
framer-motion | Page transitions and high-end visual animations
date-fns | Formatting timestamps for generated content and models
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility to cleanly merge tailwind classes

## Notes
- Tailwind Config - extend fontFamily:
  fontFamily: {
    sans: ["var(--font-sans)"],
    display: ["var(--font-display)"],
  }
- Using a dark-mode default glassmorphism aesthetic.
